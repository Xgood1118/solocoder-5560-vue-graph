图编辑器 IndexedDB 持久化完全没生效。打开页面、随便编辑一下(拖动节点、新增一个、改个属性),关掉浏览器重开,工作区直接回到默认的"流程图"模板,刚才编辑的全没了——刷新会抛 DataCloneError,IDB 写入压根没成功。导入格式也少一大半,PROMPT 要的 7 种(PNG/SVG/PDF)只接 JSON/GraphML/DOT 三种,导出来再导回去很多格式都走不通。还有节点点不动,鼠标点节点本体经常被画布上那层透明背景 rect 抢了事件,选中、拖动都受影响。检查修复一下。
